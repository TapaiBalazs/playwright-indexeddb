import { playwrightIndexeddb } from './playwright-indexeddb';

describe('playwrightIndexeddb', () => {
  it('should work', () => {
    expect(playwrightIndexeddb()).toEqual('playwright-indexeddb');
  });
});
